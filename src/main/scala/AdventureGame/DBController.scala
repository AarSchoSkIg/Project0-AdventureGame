package AdventureGame

object DBController extends DBConnecter {
 /* def createAdventurer(name: String, Health: Int = 30, attack: Int, defense: Int, loot: String, location: String) : Int = {
    val id: Int = getAdventurerID()
    connectionToDB()
    initiateDML(s"insert into adventurer (adventurerid, name, HP, attack, defense, loot, location) values ( $"
  }*/

  /*def deleteAdventurer(adventurerid: Int) : Unit ={
    connectionToDB()
    initiateDML(s"delete from adventuerer where adventurerid = $adventurerid$, $adventurername, $attack, $defense, $loot, $location;")
    disconnectFromDB()
  }
*/
}
